<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.netease.yunxin.kit.voiceroomkit.ui">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.SYSTEM_OVERLAY_WINDOW" />

    <!-- MANDATORY FOR ANDROID 14/15 FOREGROUND SERVICES -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Changed tools:targetApi to 35 to support UniApp 4.0.5 -->
    <application
        android:allowBackup="true"
        android:supportsRtl="true"
        tools:targetApi="35">

        <!-- Added hardware acceleration and specific theme handling to prevent black screens -->
        <activity
            android:name=".activity.VoiceRoomListActivity"
            android:screenOrientation="portrait"
            android:hardwareAccelerated="true"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <activity android:name=".activity.VoiceRoomCreateActivity"
            android:screenOrientation="portrait"
            android:hardwareAccelerated="true"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            />

        <activity android:name=".activity.VoiceRoomAnchorActivity"
            android:launchMode="standard"
            android:screenOrientation="portrait"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <activity android:name=".activity.VoiceRoomAudienceActivity"
            android:launchMode="standard"
            android:screenOrientation="portrait"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Added microphone type to allow background voice streaming without crashing -->
        <service
            android:name=".base.service.KeepAliveService"
            android:foregroundServiceType="mediaProjection|microphone"
            tools:replace="android:foregroundServiceType" />
    </application>

</manifest>
